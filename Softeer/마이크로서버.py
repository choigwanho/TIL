import sys 
si = sys.stdin.readline

t = int(si()) # 테스트 케이스의 개수 t

def allocate(l,arr):
    
    for i in range(l):
        arr[i]

for _ in range(2*t):
    n = int(si()) # 마이크로서버스 개수 n
    micro = list(map(int,si().split())) # 마이크로서비스 요구 메모리
    allocate(n, micro)